import { HardhatRuntimeEnvironment } from "hardhat/types";

async function deployRaffle(hre: HardhatRuntimeEnvironment) {
  const { getNamedAccounts, deployments } = hre;
}

export default deployRaffle;
